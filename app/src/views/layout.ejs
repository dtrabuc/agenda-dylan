<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || 'Agenda' %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <h1><%= heading || 'Agenda' %></h1>
      <!-- Navigation optional -->
    </header>

    <main>
      <%- body %>
    </main>

    <footer>
      <small>&copy; <%= new Date().getFullYear() %> Agenda</small>
    </footer>
  </body>
</html>
